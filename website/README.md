# Dentli Netlify Site

This folder contains the static legal, support, and contact site for Dentli.

Routes:

- `/`
- `/privacy`
- `/terms`
- `/kvkk`
- `/support`
- `/contact`

Local preview example:

```bash
npx serve website
```

Use `website` as the Netlify publish directory.
